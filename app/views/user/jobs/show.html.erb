<div class="container">
  <header class="row">
    <div class="col s12 m12 l4 left-align">
      <%= link_to "« Back to #{@job.category.name}",
                  category_path(@job.category),
                  class: "back-to-category-link" %>
    </div>
  </header>

  <%= render partial: "user/jobs/job_details" %>

  <div class="row left-align">
    <div class="col s12 m12 l12">
      <%= @job.job_status_banner_or_bid_accordion %>
    </div>
  </div>

  <%= @job.current_bids_table %>

  <%= render partial: "user/jobs/lister_details" %>
</div>

<%= render partial: "user/jobs/accordion_javascript" %>
<%= render partial: "user/jobs/google_map_javascript" %>
